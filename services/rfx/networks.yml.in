# created $(date)
version: "3.2"

networks:
  service:
    driver: overlay
    attachable: true

  service_bridge:
    driver: bridge
    ipam:
      driver: default
      config:
      -
        subnet: 172.19.0.0/16

