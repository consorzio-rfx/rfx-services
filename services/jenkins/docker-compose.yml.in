version: '3'
services:

  jenkins:
    image: '$(DOCKER_IMAGE)'
    environment:
      TZ: Europe/Rome
    ports:
      - 8080:8080
      - 50000:50000
    tty: true
    # volumes:
    #   - ${STORE_DIR}/jenkins-data:/var/jenkins_home
