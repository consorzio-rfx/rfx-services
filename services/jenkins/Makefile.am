include $(top_srcdir)/Common.mk
include $(top_srcdir)/services/docker_services.mk

DOCKER_IMAGE  = jenkins/jenkins
COMPOSER_FILE = docker-compose.yml
DIRECTORIES = ${STORE_DIR}/jenkins-data

start-local: $(DIRECTORIES)
stop-local:

